# URL absolue du backend (production ou si vous ne passez pas par le proxy Vite)
# VITE_API_BASE_URL=http://127.0.0.1:8003

# En dev, Vite proxy /api → ce serveur (php artisan serve --port=…). Adapter au port utilisé.
# VITE_API_PROXY_TARGET=http://127.0.0.1:8003
